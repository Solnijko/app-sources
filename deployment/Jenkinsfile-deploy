pipeline {
    agent any  

        stages {
        stage('Deploy') { 
            steps {
                ansiblePlaybook credentialsId: 'ssh-tomcat', extras: '-e BUILD_ID=${BUILD_ID}', inventory: 'deployment/inventory.yml', playbook: 'deployment/deploy-petclinic.yml'
            }
        }
    }
}

pipeline {
    agent any  

        stages {
        stage('Deploy') { 
            steps {
                ansiblePlaybook hostKeyChecking: false, credentialsId: 'SSH-server-credentials', extras: '-e BUILD_ID=${BUILD_ID}', inventory: 'deployment/inventory.yml', playbook: 'deployment/deploy-petclinic.yml'
            }
        }
    }
}

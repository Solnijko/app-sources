pipeline {
    agent any  
    
        stages {
        stage('Deploy') { 
            steps {
                ansiblePlaybook inventory: 'inventory.yml', playbook: 'deploy-petclinic.yml', extras: "-e BUILD_ID=${BUILD_ID}"
            }
        }
    }
}
pipeline {
    agent {
        docker {
            image any
        }
    }
    stages {
        stage('Deploy') { 
            steps {
                ansiblePlaybook inventory: 'inventory.yml', playbook: 'deploy-petclinic.yml', extras: '${BUILD_ID}'
            }
        }
    }
}
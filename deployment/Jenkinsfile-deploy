pipeline {
    agent any  

        stages {
        stage('Deploy') { 
            steps {
                ansiblePlaybook credentialsId: '8081', extras: 'BUILD_ID=${BUILD_ID}', inventory: 'deployment/inventory.yml', playbook: 'deployment/deploy-petclinic.yml'
            }
        }
    }
}